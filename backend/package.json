{
  "name": "five-elements-smoky-bookings",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute five-elements-smoky-bookings --local --file=schema.sql",
    "db:remote": "wrangler d1 execute five-elements-smoky-bookings --remote --file=schema.sql",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.26.0"
  }
}
